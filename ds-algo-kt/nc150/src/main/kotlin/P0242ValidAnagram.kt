package learn.nc150

interface P0242ValidAnagram {
    /**
     * Given two strings s and t, return true if t is an anagram of s, and false otherwise.
     * Strings s and t consist of lowercase English letters.
     */
    fun isAnagram(s: String, t: String): Boolean
}
